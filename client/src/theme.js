// theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#3a0762ff",
    },
    secondary: {
      main: "#800578",
    },
  },
});

export default theme;
