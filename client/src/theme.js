// theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#3a0762ff", // Replace with your primary color
    },
    secondary: {
      main: "#800578", // Replace with your secondary color
    },
  },
});

export default theme;
